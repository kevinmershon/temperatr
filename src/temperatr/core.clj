(ns temperatr.core)

(defn temperatr
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(defn zipcode-search
  [zipcode]
  (println
    (format "searching for zipcode %d..." zipcode)))
